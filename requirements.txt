# Core (pure-Python wheels — no compiler / Xcode needed)
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
scipy>=1.10
pyarrow>=14.0
pyyaml>=6.0
tqdm>=4.66
python-dotenv>=1.0
matplotlib>=3.7

# LLM providers (both are optional at runtime — pipeline degrades to an
# offline heuristic mode if no API keys are present, see src/trustagent/llm.py)
anthropic>=0.40
openai>=1.40
